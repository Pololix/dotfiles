# URGENT: 
- move ags and dfctl to their own repos. ags as a submodule

# Hypr
- setup hyprlock: date, hour, login panel and options + virtual keyboard (always active, one hand)
- setup modes (tablet, laptop, desktop, clamshell, etc)
- add virtual keyboard

# Nvim
- centralize ignore files in nvim
- create a dashboard with quick accesses to recent projects and fixed adresses

# AGS
- fix bluetooth power display (maybe not reactive)
- upper bar and its panels
- volume and brightness popup scrollbars
- notification integration (next to time, only when relevant)
- icon revamp
- popup for new connection or extra information (i.e. new device connected, brightness changed, etc)
- interworkspace layout that pushes hyprland inwards
- create launcher

# Dfctl
- bloat manager
- isolate .config (commited changes) from dotfiles (working directory)
- wrap system updates or package install/removal
- actions: deploy, reload

# Extras
- setup mutagen: hyprland, hyprlock, firefox
- add whatsapp pwa and background notification daemon
- spotify killswitch added as a githb submodule
