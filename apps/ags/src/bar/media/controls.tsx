export function Controls() {
    return (
        <box cssClasses={["bar-component"]} spacing={20}>
            <button onClicked={() => {
            }}>
                <label label="prev" />
            </button>

            <button onClicked={() => {
            }}>
                <label label="play" />
            </button>

            <button onClicked={() => {
            }}>
                <label label="next" />
            </button>
        </box>
    )
}
